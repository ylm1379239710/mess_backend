<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- blacklist -->
<mapper namespace="com.greate.community.dao.BlackListMapper">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="blacklistMap" type="Blacklist">
        <id column="id" property="id" jdbcType="INTEGER" />
        <id column="isAudit" property="isAudit" jdbcType="INTEGER" />
        <result column="person_involved" property="personInvolved" jdbcType="VARCHAR" />
        <result column="report_user" property="reportUser" jdbcType="VARCHAR" />
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
        <result column="behavior_description" property="behaviorDescription" jdbcType="VARCHAR" />
        <result column="wy_account" property="wyAccount" jdbcType="VARCHAR" />
        <result column="account_id" property="accountId" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->

    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        b.person_involved, b.report_user, b.create_time, b.update_time, b.behavior_description, b.wy_account, b.account_id, b.id,b.isAudit
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->

    <!-- 查询所有 -->
    <select id="findBlacklistAll" resultMap="blacklistMap">
        SELECT
        <include refid="allColumns" />
        FROM blacklist b
    </select>

    <!-- 根据条件参数查询列表 -->
    <select id="findBlacklistByCondition" resultMap="blacklistMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM blacklist b
        <where>
            <if test="key != null and key != ''">
                OR b.person_involved LIKE CONCAT('%', #{key}, '%')
            </if>
            <if test="key != null and key != ''">
                OR b.wy_account LIKE CONCAT('%', #{key}, '%')
            </if>
            <if test="key != null and key != ''">
                OR b.account_id LIKE CONCAT('%', #{key}, '%')
            </if>
        </where>
    </select>

    <!-- 根据主键查询信息 -->
    <select id="findBlacklistByIds" resultMap="blacklistMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM blacklist b WHERE b.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- 新增信息 -->
    <insert id="addBlacklist">
        INSERT INTO blacklist (
            person_involved, report_user, create_time, update_time, behavior_description, wy_account, account_id, id, isAudit
        ) VALUES (
                     #{personInvolved},
                     #{reportUser},
                     #{createTime},
                     #{updateTime},
                     #{behaviorDescription},
                     #{wyAccount},
                     #{accountId},
                     #{id},
                     #{isAudit}
                 )
    </insert>

    <!-- 批量新增信息 -->
    <insert id="addBlacklists" parameterType="list">
        INSERT INTO blacklist (
        person_involved, report_user, create_time, update_time, behavior_description, wy_account, account_id, id, isAudit
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
            #{item.personInvolved},
            #{item.reportUser},
            #{item.createTime},
            #{item.updateTime},
            #{item.behaviorDescription},
            #{item.wyAccount},
            #{item.accountId},
            #{item.id}
            #{item.isAudit}
            )
        </foreach>
    </insert>

    <!-- 修改信息 -->
    <update id="updateBlacklist">
        UPDATE blacklist
        <set>
            <if test="personInvolved != null">
                person_involved = #{personInvolved},
            </if>
            <if test="reportUser != null">
                report_user = #{reportUser},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="behaviorDescription != null">
                behavior_description = #{behaviorDescription},
            </if>
            <if test="wyAccount != null">
                wy_account = #{wyAccount},
            </if>
            <if test="accountId != null">
                account_id = #{accountId},
            </if>
            <if test="isAudit != null">
                isAudit = #{isAudit},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 批量修改信息 -->
    <update id="updateBlacklists" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE blacklist
            <set>
                <if test="item.personInvolved != null">
                    person_involved = #{item.personInvolved},
                </if>
                <if test="item.reportUser != null">
                    report_user = #{item.reportUser},
                </if>
                <if test="item.createTime != null">
                    create_time = #{item.createTime},
                </if>
                <if test="item.updateTime != null">
                    update_time = #{item.updateTime},
                </if>
                <if test="item.behaviorDescription != null">
                    behavior_description = #{item.behaviorDescription},
                </if>
                <if test="item.wyAccount != null">
                    wy_account = #{item.wyAccount},
                </if>
                <if test="item.accountId != null">
                    account_id = #{item.accountId},
                </if>
                <if test="item.isAudit != null">
                    isAudit = #{item.isAudit},
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>

    <!-- 根据主键删除 -->
    <delete id="deleteBlacklistById" parameterType="string">
        DELETE FROM blacklist WHERE id = #{id}
    </delete>

    <!-- 根据主键批量删除 -->
    <delete id="deleteBlacklistByIds" parameterType="list">
        DELETE FROM blacklist WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>